import 'package:flutter/material.dart';

class AppColors {
  // --- Dark Mode Palette ---
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceHighlight = Color(0xFF2C2C2C);

  // --- Light Mode Palette ---
  static const Color backgroundLight = Color(0xFFF8F9FA);
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceHighlightLight = Color(0xFFEFEFEF);

  // Accents (The Slice Pop)
  static const Color primary = Color(0xFFB9FF66); // Neon Green
  static const Color primaryDark = Color(0xFF8BCC4A);
  static const Color deepElectric = Color(0xFF5D33F8); // Slice Purple Accent

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white54;
  static const Color textPrimaryDark = Color(0xFF1A1A1A);
  static const Color textSecondaryDark = Color(0xFF757575);

  // States
  static const Color error = Color(0xFFFF5252);
  static const Color success = Color(0xFF4CAF50);
}
